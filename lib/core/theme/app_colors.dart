import 'package:flutter/material.dart';

class AppColors {
  // Primary CTAs only (Buy Ticket, Join Live, Remind Me)
  static const Color primaryAccent = Color(0xFFFB8500);

  // Highlights, badges, live indicators
  static const Color secondaryAccent = Color(0xFFFFB703);

  // Icons, links, speaker socials
  static const Color brandBlue = Color(0xFF219EBC);

  // Headers, navbars, overlays, app bars
  static const Color darkContrast = Color(0xFF023047);

  // Card backgrounds, secondary sections
  static const Color supportBlue = Color(0xFF8ECAE6);

  // Additional utility colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
  
  // Text colors
  static const Color textPrimary = white;
  static const Color textSecondary = supportBlue;
}
